---
title: 'Confirm Booking'
openapi: 'POST /api/bookings/{id}/confirm'
---

Airline confirms, rejects, or provides alternative for booking (FFA semantics).

## Path Parameters

<ParamField path="id" type="string" required>
  Booking identifier
</ParamField>

## Request Body

<ParamField body="BookingConfirmationRequest" type="object" required>
  Booking confirmation request

  <Expandable title="Confirmation Request Properties">
    <ParamField body="action" type="string" required>
      Action: 'confirm', 'reject', or 'alternate'
    </ParamField>
    <ParamField body="reason" type="string">
      Reason for rejection (if applicable)
    </ParamField>
    <ParamField body="alternateFlightId" type="string">
      Alternative flight ID (if alternate action)
    </ParamField>
  </Expandable>
</ParamField>

## Response (200)

<ParamField status="200" type="BookingConfirmationResponse">
  <ParamField name="bookingId" type="string">
    Booking identifier
  </ParamField>
  <ParamField name="status" type="string">
    Updated booking status
  </ParamField>
  <ParamField name="awbNumber" type="string">
    Confirmed AWB number
  </ParamField>
</ParamField>

## Response (400)

<ParamField status="400" type="ErrorResponse">
  <ParamField name="error" type="string">
    Invalid request
  </ParamField>
</ParamField>
